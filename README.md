Keep left.jpeg, right.jpeg, and uncalibrated_stereo.py in the same folder.
Install dependencies:
pip install opencv-python numpy matplotlib
Run:
python uncalibrated_stereo.py
Click the same point on the object in both images when prompted.
The program prints F,E,R and distance, and saves the result images.
